package com.skala.miniproject.domain.analysis;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FillerBreakdownRepository extends JpaRepository<FillerBreakdown, Long> {
}
